import React from 'react'
import { CircularProgress } from '@mui/material'

export default () => <div style={{
      height: '100vh',
      width: '100vw',
      display: 'grid',
      placeItems: 'center'
    }}>
      <CircularProgress />
    </div>